# Translate_CLI

First, you need to configure config-example.yaml, which is the key and secret of Youdao Translation API, and then rename config-example.yaml to config.yaml
Then, execute tran.exe

For example,you can use like `tran.exe word`,you will get "词"
If you want to convert Chinese to English, you can enter `tran.exe -p ce 词`